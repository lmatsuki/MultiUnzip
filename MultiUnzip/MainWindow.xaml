<Window x:Class="MultiUnzip.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MultiUnzip"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <DockPanel>
        <ToolBarTray DockPanel.Dock="Top">
            <ToolBar>
                <Button Name="unzipButton" Content="Unzip" Click="UnzipButton_Click" Width="50" />
                <Button Name="clearAllButton" Content="Clear All" Click="ClearAllButton_Click" Width="50" />
            </ToolBar>
        </ToolBarTray>
        <Label DockPanel.Dock="Bottom" Content="Drag and drop files above and click Unzip!" />
        <ListBox Name="filesToUnzip" AllowDrop="True" DisplayMemberPath="FileName" DragDrop.Drop="UnzipFiles_Drop" Margin="5">
            
        </ListBox>
    </DockPanel>
</Window>
